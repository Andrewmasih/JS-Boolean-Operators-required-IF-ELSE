/* If the condition is TRUE, then the IF statement will be executed, otherwise, the ELSE code will be RUN */

let a = 10;
let b = 20;
let result;

if (a<b) {
  result = "a is smaller";
} else {
  result = "a is not smaller";
}

console.log(result);

/* Try changing the condition values and see the result */